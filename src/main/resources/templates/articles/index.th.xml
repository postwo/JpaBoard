<?xml version="1.0"?>
<thlogic>
    <attr sel="#header" th:replace="header :: header" />
    <attr sel="#footer" th:replace="footer :: footer" />

    <attr sel="#article-table">
        <attr sel="tbody" th:remove="all-but-first"> <!-- th:remove="all-but-first" 이부분은 tbody에있는 tr태그 첫번째거 빼고 다지운다는 뜻이다-->
            <attr sel="tr[0]" th:each="article : ${articles}"> <!--첫번째 tr을 foreach를 해준다는 뜻이다 --> <!-- 결론적으로 밑의 있는 것들을 첫벌째 th태그의 대입 한다는 뜻이다 -->
                <attr sel="td.title/a" th:text="${article.title}" th:href="@{'/articles/' + ${article.id}}" />
                <attr sel="td.hashtag" th:text="${article.hashtag}" />
                <attr sel="td.user-id" th:text="${article.nickname}" />
                <attr sel="td.created-at/time" th:datetime="${article.createdAt}" th:text="${#temporals.format(article.createdAt, 'yyyy-MM-dd')}" />
            </attr>
        </attr>
    </attr>
</thlogic>